# med-equipment-platform

Проект в формате frontend-only:
- `frontend/` — Vite + React сайт

## Быстрый старт

1. Установить зависимости:
   - `npm install --prefix frontend`
2. Запустить frontend: `npm run dev:frontend`

## Проверка кода

- `npm run check`
